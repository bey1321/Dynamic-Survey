{
  "name": "dynamic-survey-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
