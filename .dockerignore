# Dependencies — installed inside the image
**/node_modules

# Build artifacts
client/dist

# Local environment files — pass secrets via docker run -e or --env-file
**/.env
**/.env.*

# Version control
.git
.gitignore

# Editor / OS noise
.vscode
.idea
*.log
.DS_Store
Thumbs.db

# Development-only files
server/tmpclaude-*
